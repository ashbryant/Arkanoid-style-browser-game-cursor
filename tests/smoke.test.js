// Smoke test — verifies the test runner itself is working
// More comprehensive tests are added per milestone

describe('test runner', () => {
  it('should be alive', () => {
    expect(1 + 1).toBe(2)
  })
})
